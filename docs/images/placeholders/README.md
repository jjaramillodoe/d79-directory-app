Placeholder directory for Mintlify screenshots.

Until real captures exist, MDX pages use:

```md
![Descriptive Alt Text](https://placehold.co/600x400)
```

Replace those URLs with production screenshots when they are ready. Logo and favicon in `mint.json` / `docs.json` also point at the same placeholder for now.
